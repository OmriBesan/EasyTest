<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application

        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher_background"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EasyTest">
    <meta-data
        android:name="com.google.android.geo.API_KEY"
        android:value="@string/google_maps_key" />

        >
        <activity
            android:name=".ui.owner.ManageScheduleActivity"
            android:exported="false" />
        <activity
            android:name=".ui.booking.MyReservationsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.login.SignUpActivity"
            android:exported="false"
            android:theme="@style/Theme.EasyTest" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.EasyTest">
        </activity>
        <activity android:name=".ui.location.LocationPermissionActivity" />

        <activity
            android:name=".ui.onboarding.OnboardingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.location.EnterLocationManuallyActivity" />

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.EasyTest" />
        <activity
            android:name=".ui.restaurant.RestaurantListActivity"
            android:exported="false" />
        <activity android:name=".ui.restaurant.RestaurantDetailsActivity" />
        <activity android:name=".ui.booking.ReserveActivity" />
    </application>

</manifest>
